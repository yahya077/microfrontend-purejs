{
  "name": "layout",
  "version": "1.0.0",
  "description": "This is layout section of micro-frontend",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "build": "rollup src/client.js --file build/fragment.js --format iife",
    "server": "node --experimental-modules ./src/server.js",
    "start": "npm run build; npm run server",
    "webpack-build": "npm run webpack",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Yahya Hindioğlu business@yahyahindioglu.com",
  "license": "ISC",
  "dependencies": {
    "ejs": "^2.7.4",
    "express": "^4.17.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "rollup": "^1.32.1"
  }
}
